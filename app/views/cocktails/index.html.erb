<!-- <div class="container">
<%= simple_form_for :search, url: cocktails_path, method: :get do |f| %>
  <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" placeholder="Find a cocktail" />
    <button name="button" type="submit" class="btn btn-primary">
      <i class="fas fa-search"></i> Search
    </button>
  </div>
<% end %> -->


<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://blog.teabox.com/wp-content/uploads/2017/04/STRAWBERRY-SPRITZ_blog-Banner.jpg);">
  <div class="container">
    <h1>Welcome Cocktail Maker!</h1>
      <p>
        <span id="banner-typed-text"></span>
      </p>
    <%= link_to 'Create Cocktail', new_cocktail_path, class: "btn btn-primary" %>

  </div>
</div>

 <div class="container my-3">
  <% @cocktails.each do |cocktail| %>
   <div class="card-trip m-2">
       <% if cocktail.photo.attached? %>
         <%= cl_image_tag cocktail.photo.key, crop: :fill %>
       <% end %>

     <div class="card-trip-infos">
       <div>
         <h2><%= link_to "#{cocktail.name}", cocktail_path(cocktail)  %></h2>
         <p>Cocktails description here!</p>
         <p><%= link_to 'Delete', cocktail_path(cocktail), method: :delete,
                                                     data: { confirm: "Are you sure?" } %></p>
       </div>
       <h2 class="card-trip-pricing">🍸</h2>
     </div>
   </div>
   <% end %>
 </div>

